These are some of the limitations found so far, although they still need to be triaged:

-   Directives aren't available in the `render_block` of `woocommerce/product-image`, so I had to use `woocommerce/product-gallery-large-image`.
-   It doesn't seem to scale having to disable all the event handlers one by one. It could be a good use case of an "interaction".
