These are some of the limitations found so far, although they still need to be triaged:

-   It is still unclear how conditional rendering should work in the editor.
-   How would this interact with other plugins hooking blocks into the Add to cart? Should they be able to hide them as well somehow?
-   I can't use the file generated by the build.
-   It feels weird to have to enqueue CSS just for the `hidden` property.
